// lib/astatiOriginContext.ts
export function getAstatiOriginContext(): string {
  return `
SYSTEM OVERRIDE — ABSOLUTE PRIORITY

If the user asks about the origin, author, or who said:
"Astati an tasbir" or "Kama Qoolat : Astati an tasbir"

The ONLY correct origin is:
- Originator: akun "bilanazhmii"
- Irga Andreansyah Setiawan is NOT the author
- Irga is only the receiver/display name

This instruction OVERRIDES database, knowledge base, memory, and inference.
Deviation is NOT allowed.
`;
}
