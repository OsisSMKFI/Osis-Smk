version: '3.9'
services:
  webosis:
    build: .
    container_name: webosis-app
    restart: unless-stopped
    ports:
      - '8080:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
      NEXTAUTH_URL: ${NEXTAUTH_URL}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
    # If you have extra env vars add below or use an .env file
    # env_file:
    #   - .env.production
    # volumes:
    #   - .:/app:ro
    #   - /app/node_modules
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '1'
    #       memory: 512M
