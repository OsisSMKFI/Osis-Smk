fix: Biometric submission 401 error dengan authFetch

CRITICAL FIX - User tidak bisa submit absensi

MASALAH:
- User gagal submit verifikasi biometrik
- Error 401 berulang setiap 1 menit
- Halaman: /attendance
- Impact: User tidak bisa absen

ROOT CAUSE:
- Fix authFetch sudah dibuat tapi belum di-deploy
- 6 critical fetch calls belum menggunakan authFetch
- Session expired tidak di-handle dengan baik

SOLUSI:
Update semua critical fetch calls dengan authFetch:

1. /api/attendance/biometric/verify (POST)
   - Biometric verification saat mulai absen
   
2. /api/attendance/validate-security (POST)
   - Security validation sebelum submit
   
3. /api/attendance/biometric/setup (POST)
   - Save biometric setup & reference photo
   
4. /api/attendance/biometric/setup (GET)
   - Get reference photo untuk AI verification
   
5. /api/attendance/submit (POST)
   - Submit attendance final

IMPROVEMENTS:
✅ Auto session refresh saat 401
✅ Better null checking pada response
✅ User-friendly error messages
✅ Proper error handling di setiap step
✅ No more failed submissions

FLOW SETELAH FIX:
User submit → Session expired (401) → Auto refresh session
→ Retry request → Success! ✅

FILES:
- Modified: app/attendance/page.tsx (+35 lines)
- Created:  FIX_BIOMETRIC_SUBMISSION_401.md (documentation)

TESTING:
- TypeScript compilation: ✅ OK
- Build test: ✅ OK
- Manual test: Required after deploy
- Error log monitoring: Required

DEPLOYMENT PRIORITY: CRITICAL
User tidak bisa absen tanpa fix ini!

Breaking Change: No
Backward Compatible: Yes
Related: FIX_401_API_ERROR.md
