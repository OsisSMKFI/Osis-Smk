[phases.setup]
nixPkgs = ["nodejs_20", "git"]

[phases.install]
cmds = ["npm ci || npm install"]

# Skip build - using pre-built dist
[phases.build]
cmds = ["echo 'Using pre-built dist'"]

[start]
cmd = "node dist/server.js"
